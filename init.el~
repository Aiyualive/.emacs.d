; -----------------------------
; |         RICEMACS          |
; -----------------------------

;; Added by Package.el.
; (package-initialize)

(load-file "~/.emacs.d/ricemacs/repositories.el")

(load-file "~/.emacs.d/ricemacs/latex.el")

(load-file "~/.emacs.d/ricemacs/interface.el")

(load-file "~/.emacs.d/ricemacs/keybindings.el")

(load-file "~/.emacs.d/ricemacs/theme.el")

(setq custom-file "~/.emacs.d/custom.el")
(load custom-file)
