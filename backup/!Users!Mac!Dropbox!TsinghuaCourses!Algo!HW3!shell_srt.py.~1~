def shell_srt(a):
    n = len(a)
    k = int(n/2)
    while k > 0:
        for i in range(k,n):

            x = a[i]
            j = i
            while  j >= k and a[j-k] > x:
                a[j] = a[j-k]
                j -= k
            a[j] = x
        k = int(k/2)
    return a
