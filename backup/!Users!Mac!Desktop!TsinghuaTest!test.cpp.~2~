#include <iostream>#include <string>using namespace std;int main(int argc, char* argv[]) {  ifstream myfile(argv[1]);  string data;  while(! myfile.eof()) {    getline(myfile, data);    cout << data << "\n\n";  }}